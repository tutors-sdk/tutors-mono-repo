# Rule ids that are never used again (see guides/EARS-METHODOLOGY.md, Rule ids).
# One four-digit id per line, e.g. 0031. A Rule that is deleted adds its id here in the same commit;
# the audit fails a Rule that carries one of these ids.
