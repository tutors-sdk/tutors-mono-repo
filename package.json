{
  "name": "tutors-mono-repo",
  "version": "1.0.0",
  "private": true,
  "description": "Tutors monorepo - educational course reader platform",
  "scripts": {
    "dev": "npm run dev --workspace=apps/reader",
    "build": "npm run build --workspace=apps/reader",
    "check": "npm run check --workspace=apps/reader"
  },
  "devDependencies": {
    "clsx": "^2.1.1",
    "esm-env": "^1.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
