apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The reader overlay plus its external entry point on this substrate.
resources:
  - ../../../overlays/reader

components:
  - ../../../components/ingress
