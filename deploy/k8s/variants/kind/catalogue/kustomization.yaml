apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The catalogue overlay plus its external entry point on this substrate.
resources:
  - ../../../overlays/catalogue

components:
  - ../../../components/ingress
