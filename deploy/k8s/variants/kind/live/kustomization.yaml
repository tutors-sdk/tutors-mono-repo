apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The live overlay plus its external entry point on this substrate.
resources:
  - ../../../overlays/live

components:
  - ../../../components/ingress
