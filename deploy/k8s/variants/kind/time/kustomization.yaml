apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The time overlay plus its external entry point on this substrate.
resources:
  - ../../../overlays/time

components:
  - ../../../components/ingress
