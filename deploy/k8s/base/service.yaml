apiVersion: v1
kind: Service
metadata:
  name: tutors-app
  labels:
    app.kubernetes.io/part-of: tutors
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tutors-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
