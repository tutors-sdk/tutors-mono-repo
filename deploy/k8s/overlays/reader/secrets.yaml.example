# Copy to secrets.yaml (git-ignored), fill in, then:
#   oc apply -f deploy/k8s/overlays/reader/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: reader-tutors-app-oauth
type: Opaque
stringData:
  PRIVATE_AUTH_GITHUB_ID: ""
  PRIVATE_AUTH_GITHUB_SECRET: ""
  # At least 32 characters: openssl rand -base64 32
  PRIVATE_AUTH_SECRET: ""
  # Supabase service_role key for the reader's /api routes (Project settings → API).
  # It bypasses Row-Level Security: only ever in this Secret, never in the ConfigMap.
  PRIVATE_SUPABASE_SERVICE_ROLE_KEY: ""
