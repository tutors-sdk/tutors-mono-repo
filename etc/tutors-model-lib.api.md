## API Report File for "@tutors/tutors-model-lib"

> Do not edit this file. It is a report generated by [api-surface](../scripts/api-surface.ts).
>
> Package: `@tutors/tutors-model-lib`
> Entry point: `src/tutors.ts`

```ts

// --- interfaces ---

// (from src/types/type-utils.ts)
export interface LearningRecord;


// --- types ---

// (from src/types/learning-objects.ts)
export type Archive = Lo & { type: "archive"; archiveFile?: string; };

// (from src/types/calendar-types.ts)
export type Calendar = { title: string; weeks: WeekType[]; currentWeek?: WeekType; };

// (from src/types/learning-objects.ts)
export type Composite = Lo & { toc: Lo[]; los: Lo[]; panels: Panels; units: Units; };

// (from src/types/learning-objects.ts)
export type Course = Composite & { type: "course"; courseId: string; courseUrl: string; topicIndex: Map<string, Topic>; loIndex: Map<string, Lo>; walls?: Lo[][]; wallMap?: Map<string, Lo[]>; properties: Properties; calendar?: Properties; enrollment?: Enrollment; courseCalendar?: Calendar; authLevel: number; isPortfolio: boolean; isPrivate: boolean; llm: number; pdfOrientation: string; areVideosHidden: boolean; areLabStepsAutoNumbered: boolean; hasEnrollment: boolean; hasCalendar: boolean; defaultPdfReader: string; footer: string; ignorePin: string; owner?: string; lecturers?: string[]; companions: IconNavBar; wallBar: IconNavBar; };

// (from src/types/tutors-id-types.ts)
export type CourseSentimentId = (typeof COURSE_SENTIMENT_IDS)[number];

// (from src/types/calendar-types.ts)
export type Enrollment = { whitelist: string[]; students: Student[]; };

// (from src/types/learning-objects.ts)
export type Github = Lo & { type: "github"; };

// (from src/types/icon-types.ts)
export type IconNav = { link: string; type: string; tip: string; target: string; };

// (from src/types/icon-types.ts)
export type IconNavBar = { show: boolean; bar: IconNav[]; };

// (from src/types/icon-types.ts)
export type IconType = { type: string; color: string; };

// (from src/types/learning-objects.ts)
export type Lab = Lo & { type: "lab"; los: LabStep[]; pdf: string; pdfFile: string; };

// (from src/types/learning-objects.ts)
export type LabStep = { title: string; shortTitle: string; contentMd: string; contentHtml?: string; route: string; id: string; parentLo?: Lab; type: string; };

// (from src/types/learning-objects.ts)
export type Lo = { type: string; id: string; title: string; summary: string; contentMd: string; frontMatter: Properties; contentHtml?: string; route: string; authLevel: number; img: string; imgFile: string; icon?: IconType; video: string; videoids: VideoIdentifiers; hide: boolean; parentLo?: Lo; parentTopic?: Topic; parentCourse?: Course; breadCrumbs?: Lo[]; learningRecords?: Map<string, LearningRecord>; };

// (from src/types/type-utils.ts)
export type LoType = (typeof loTypes)[number];

// (from src/types/learning-objects.ts)
export type Note = Lo & { type: "note"; };

// (from src/types/learning-objects.ts)
export type Notebook = Lo & { type: "notebook"; cells: NotebookCell[]; kernelLanguage: string; kernelName: string; };

// (from src/types/learning-objects.ts)
export type NotebookCell = { cellType: "markdown" | "code" | "raw"; source: string; sourceHtml?: string; outputs: NotebookOutput[]; outputsHtml?: string; executionCount: number | null; metadata: Record<string, unknown>; id: string; };

// (from src/types/learning-objects.ts)
export type NotebookOutput = { outputType: "stream" | "execute_result" | "display_data" | "error"; text?: string; data?: Record<string, string>; traceback?: string[]; name?: string; executionCount?: number | null; };

// (from src/types/learning-objects.ts)
export type PanelNote = Lo & { type: "panelnote"; };

// (from src/types/learning-objects.ts)
export type Panels = { panelVideos: PanelVideo[]; panelTalks: PanelTalk[]; panelNotes: PanelNote[]; panelPodcasts: Podcast[]; };

// (from src/types/learning-objects.ts)
export type PanelTalk = Talk & { type: "paneltalk"; };

// (from src/types/learning-objects.ts)
export type PanelVideo = Lo & { type: "panelvideo"; };

// (from src/types/learning-objects.ts)
export type Podcast = Lo & { type: "podcast"; episode: PodcastEpisodeIdentifier; };

// (from src/types/media-types.ts)
export type PodcastEpisodeIdentifier = { service: string; id: string; };

// (from src/services/search.ts)
export type ResultType = { fenced: boolean; language: string; contentMd: string; lab: Lo; html: string; title: string; link: string; };

// (from src/types/learning-objects.ts)
export type Side = Composite & { type: "side"; };

// (from src/types/calendar-types.ts)
export type Student = { name:string, id:string };

// (from src/types/learning-objects.ts)
export type Talk = Lo & { type: "talk"; pdf: string; pdfFile: string; };

// (from src/types/learning-objects.ts)
export type Topic = Composite & { type: "topic"; };

// (from src/types/learning-objects.ts)
export type Tutorial = Lo & { type: "tutorial"; pdf: string; pdfFile: string; };

// (from src/types/tutors-id-types.ts)
export type TutorsId = { name: string; login: string; email: string; image: string; share: string; sentiment: string; };

// (from src/types/learning-objects.ts)
export type Unit = Composite & { type: "unit"; };

// (from src/types/learning-objects.ts)
export type Units = { units: Unit[]; sides: Side[]; standardLos: Lo[]; };

// (from src/types/media-types.ts)
export type VideoIdentifier = { service: string; id: string; url?: string; externalUrl?: string; };

// (from src/types/media-types.ts)
export type VideoIdentifiers = { videoid: string; videoIds: VideoIdentifier[]; };

// (from src/types/learning-objects.ts)
export type Web = Lo & { type: "web"; };

// (from src/types/calendar-types.ts)
export type WeekType = { title: string; type: string; date: string; dateObj: Date; };

// (from src/types/learning-objects.ts)
export type Whiteboard = Lo & { type: "whiteboard"; excalidraw: string; excalidrawFile: string; };


// --- classes ---

// (from src/types/type-utils.ts)
export class Properties;


// --- functions ---

// (from src/utils/lo-utils.ts)
export function allVideoLos(los: Lo[]) : Lo[];

// (from src/utils/markdown-utils.ts)
export function convertLabToHtml(course: Course, lab: Lab, protocol: string);

// (from src/utils/markdown-utils.ts)
export function convertLoToHtml(course: Course, lo: Lo, protocol: string);

// (from src/utils/markdown-utils.ts)
export function convertMdToHtml(md: string, codeTheme: string) : string;

// (from src/utils/markdown-utils.ts)
export function convertNoteToHtml(course: Course, note: Note, protocol: string);

// (from src/utils/course-utils.ts)
export function createCompanions(course: Course);

// (from src/utils/course-utils.ts)
export function createToc(course: Course);

// (from src/utils/course-utils.ts)
export function createWalls(course: Course);

// (from src/utils/lo-utils.ts)
export function crumbs(lo: Lo | undefined, los: Lo[]);

// (from src/services/lo-tree.ts)
export function decorateCourseTree( course: Course, courseId: string, courseUrl, );

// (from src/services/lo-tree.ts)
export function decorateLoTree(course: Course, lo: Lo);

// (from src/services/search.ts)
export function extractPath(astring: string) : string;

// (from src/utils/markdown-utils.ts)
export function filter(src: string, url: string, protocol: string) : string;

// (from src/utils/lo-utils.ts)
export function filterByType(list: Lo[], type: LoType) : Lo[];

// (from src/utils/lo-utils.ts)
export function fixRoutePaths(lo: Lo);

// (from src/utils/lo-utils.ts)
export function flattenLos(los: Lo[]) : Lo[];

// (from src/utils/lo-utils.ts)
export function getPanels(los: Lo[]) : Panels;

// (from src/utils/lo-utils.ts)
export function getUnits(los: Lo[]) : Units;

// (from src/utils/lo-utils.ts)
export function getVideoConfig(lo: Lo) : VideoIdentifier;

// (from src/utils/course-utils.ts)
export function initCalendar(course: Course);

// (from src/utils/markdown-utils.ts)
export function initHighlighter(codeHighlighter: any);

// (from src/utils/lo-utils.ts)
export function injectCourseUrl(los: Lo[], id: string, url: string);

// (from src/types/type-utils.ts)
export function isCompositeLo(lo: Lo) : boolean;

// (from src/services/search.ts)
export function isValid(str: string) : boolean;

// (from src/utils/lo-utils.ts)
export function loadIcon(lo: Lo) : IconType | undefined;

// (from src/utils/course-utils.ts)
export function loadPropertyFlags(course: Course);

// (from src/utils/lo-utils.ts)
export function removeLeadingHashes(str: string) : string;

// (from src/utils/lo-utils.ts)
export function removeUnknownLos(los: Lo[]);

// (from src/services/search.ts)
export function searchHits(los: Lo[], searchTerm: string) : ResultType[];

// (from src/utils/lo-utils.ts)
export function setShowHide(lo: Lo, status: boolean);

// (from src/utils/lo-utils.ts)
export function sortLos(los: Array<Lo>) : Lo[];


// --- consts ---

// (from src/types/media-types.ts)
export const assetTypes: string[];

// (from src/types/tutors-id-types.ts)
export const COURSE_SENTIMENT_IDS;

// (from src/types/media-types.ts)
export const imageTypes: string[];

// (from src/types/type-utils.ts)
export const loCompositeTypes: string[];

// (from src/types/type-utils.ts)
export const loTypes: string[];

// (from src/utils/markdown-utils.ts)
export const markdownIt: MarkdownIt;

// (from src/types/type-utils.ts)
export const preOrder: Map<string, number>;

// (from src/types/type-utils.ts)
export const simpleTypes: string[];

```

### Summary

| Kind | Count |
|------|-------|
| class | 1 |
| const | 8 |
| function | 31 |
| interface | 1 |
| type | 38 |
| **Total** | **79** |
